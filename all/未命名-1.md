```mermaid
graph TD
    A[开始] --> B[目标检测模型改进]
    B --> C{NanoDet-Plus基础模型}
    C --> D[数据增强策略改进]
    D --> E[引入CBAM注意力模块]
    E --> F[替换Backbone为EfficientNet]
    F --> G[更换激活函数为Swish或HardSwish]
    G --> H[模型评估]
    H --> I[检测精度评估<br>(mAP, 精确度, 召回率)]
    I --> J[实时性评估<br>(推理速度)]
    J --> K[鲁棒性评估<br>(不同飞行环境下的表现)]
    K --> L[无人机导航系统]
    L --> M[路径规划与避障]
    M --> N[全局规划算法<br>(A*算法)]
    N --> O[局部避障算法<br>(动态窗口法DWA)]
    O --> P[综合全局规划与局部避障]
    P --> Q[动态、实时路径规划系统]
    Q --> R[结束]

    style A fill:#f9f,stroke:#333,stroke-width:2px
    style R fill:#f9f,stroke:#333,stroke-width:2px

```